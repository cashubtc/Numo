<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_bg_white">

    <!-- Top App Bar -->
    <com.electricdreams.numo.ui.components.NumoTopBar
        android:id="@+id/top_bar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/top_bar_height"
        android:background="@color/color_bg_white"
        app:topBarTitle="@string/history_toolbar_title"
        app:topBarNavIcon="@drawable/ic_close"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/overflow_button"
        android:layout_width="@dimen/top_bar_height"
        android:layout_height="@dimen/top_bar_height"
        android:padding="12dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/transaction_detail_overflow_menu"
        android:src="@drawable/ic_more_vert"
        app:tint="@color/color_text_primary"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ImageButton
        android:id="@+id/insights_button"
        android:layout_width="@dimen/top_bar_height"
        android:layout_height="@dimen/top_bar_height"
        android:padding="12dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/insights_open"
        android:src="@drawable/ic_insights"
        app:tint="@color/color_text_primary"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/overflow_button" />

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/history_coordinator"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/top_bar">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/history_app_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/color_bg_white"
            android:stateListAnimator="@null"
            app:elevation="0dp"
            app:liftOnScroll="false">

            <!-- Balance Summary: scrolls away with the list -->
            <LinearLayout
                android:id="@+id/balance_section"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:orientation="vertical"
                android:paddingTop="@dimen/space_l"
                android:paddingBottom="@dimen/space_xl"
                app:layout_scrollFlags="scroll">

                <!-- "Total Balance" label -->
                <TextView
                    android:id="@+id/balance_label"
                    style="@style/Text.Caption"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/history_total_balance_label" />

                <!-- Large fiat balance -->
                <TextView
                    android:id="@+id/balance_fiat"
                    style="@style/Text.DisplayAmount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/space_xs"
                    android:textColor="@color/color_text_primary"
                    tools:text="$15.29" />

                <!-- Sat balance subtitle -->
                <TextView
                    android:id="@+id/balance_sats"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:textAppearance="@style/Text.Body"
                    android:textColor="@color/color_text_tertiary"
                    tools:text="₿21,305" />

            </LinearLayout>

            <!-- Filter row: pinned under the top bar. Active filters as removable chips,
                 then the Filter pill that opens the filter sheet. -->
            <LinearLayout
                android:id="@+id/filter_row"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/color_bg_white"
                android:gravity="center_vertical"
                android:minHeight="52dp"
                android:orientation="horizontal"
                android:paddingStart="@dimen/space_l"
                android:paddingEnd="@dimen/space_l">

                <HorizontalScrollView
                    android:id="@+id/active_filters_scroll"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:fadingEdgeLength="@dimen/space_l"
                    android:overScrollMode="never"
                    android:requiresFadingEdge="horizontal"
                    android:scrollbars="none">

                    <com.google.android.material.chip.ChipGroup
                        android:id="@+id/active_filter_chips"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        app:chipSpacingHorizontal="@dimen/space_s"
                        app:singleLine="true" />
                </HorizontalScrollView>

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/filter_button"
                    style="@style/Widget.Numo.Button.FilterPill"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginStart="@dimen/space_s"
                    android:text="@string/history_filter_button"
                    app:icon="@drawable/ic_tune" />
            </LinearLayout>

            <!-- Hairline shown once the balance has scrolled away -->
            <View
                android:id="@+id/filter_row_divider"
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:alpha="0"
                android:background="@color/color_divider" />

        </com.google.android.material.appbar.AppBarLayout>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <!-- List (month headers + transaction items) -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/history_recycler_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:paddingBottom="@dimen/space_l" />

            <!-- Empty View -->
            <include
                android:id="@+id/empty_view"
                layout="@layout/component_empty_state"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:visibility="gone" />

        </FrameLayout>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</androidx.constraintlayout.widget.ConstraintLayout>