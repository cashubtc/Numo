<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_bg_white">

    <!-- Top Bar -->
    <LinearLayout
        android:id="@+id/top_bar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/top_bar_height"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="@dimen/space_s"
        android:elevation="1dp"
        android:background="@color/color_bg_white"
        app:layout_constraintTop_toTopOf="parent">

        <ImageButton
            android:id="@+id/back_button"
            android:layout_width="@dimen/icon_size_large"
            android:layout_height="@dimen/icon_size_large"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_chevron_back"
            android:contentDescription="@string/tips_settings_toolbar_back"
            app:tint="@color/color_text_primary" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/tips_settings_title"
            android:textSize="22sp"
            android:textColor="@color/color_text_primary"
            android:fontFamily="sans-serif-medium"
            android:textAlignment="center" />

        <!-- Spacer to balance layout -->
        <View
            android:layout_width="48dp"
            android:layout_height="48dp" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/top_bar"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="@dimen/space_2xl">

            <!-- Description -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingHorizontal="@dimen/settings_horizontal_padding"
                android:paddingTop="@dimen/space_l"
                android:paddingBottom="@dimen/space_s"
                android:text="@string/tips_settings_description"
                android:textSize="14sp"
                android:textColor="@color/color_text_secondary"
                android:lineSpacingExtra="2dp" />

            <!-- Enable Tips Switch -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="72dp"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="@dimen/settings_horizontal_padding"
                android:background="?attr/selectableItemBackground">

                <ImageView
                    android:layout_width="@dimen/icon_size_small"
                    android:layout_height="@dimen/icon_size_small"
                    android:layout_marginEnd="@dimen/space_l"
                    android:src="@drawable/ic_tip"
                    app:tint="@color/color_primary_green" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/tips_settings_enable_label"
                    android:textSize="17sp"
                    android:textColor="@color/color_text_primary"
                    android:fontFamily="sans-serif-medium" />

                <androidx.appcompat.widget.SwitchCompat
                    android:id="@+id/tips_enabled_switch"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:thumbTint="@color/selector_switch_thumb"
                    app:trackTint="@color/selector_switch_track" />
            </LinearLayout>

            <!-- Presets Section -->
            <LinearLayout
                android:id="@+id/presets_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone">

                <!-- Section Header -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingHorizontal="@dimen/settings_horizontal_padding"
                    android:paddingTop="@dimen/space_xl"
                    android:paddingBottom="@dimen/space_s"
                    android:text="@string/tips_settings_presets_header"
                    style="@style/Text.SectionHeader" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingHorizontal="@dimen/settings_horizontal_padding"
                    android:paddingBottom="@dimen/space_m"
                    android:text="@string/tips_settings_presets_description"
                    android:textSize="13sp"
                    android:textColor="@color/color_text_tertiary" />

                <!-- Card Container for Presets -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginHorizontal="@dimen/space_l"
                    android:orientation="vertical"
                    android:background="@drawable/bg_rounded_card_large">

                    <!-- Presets List -->
                    <LinearLayout
                        android:id="@+id/presets_list"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical" />

                </LinearLayout>

                <!-- Add Preset Button -->
                <LinearLayout
                    android:id="@+id/add_preset_button"
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/settings_item_height"
                    android:layout_marginTop="@dimen/space_s"
                    android:layout_marginHorizontal="@dimen/space_l"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:paddingHorizontal="@dimen/space_l"
                    android:background="@drawable/bg_rounded_card_large"
                    android:clickable="true"
                    android:focusable="true">

                    <ImageView
                        android:layout_width="@dimen/icon_size_small"
                        android:layout_height="@dimen/icon_size_small"
                        android:layout_marginEnd="@dimen/space_l"
                        android:src="@drawable/ic_add"
                        app:tint="@color/color_primary_green" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/tips_settings_add_preset"
                        android:textSize="16sp"
                        android:textColor="@color/color_primary_green"
                        android:fontFamily="sans-serif-medium" />
                </LinearLayout>

                <!-- Reset to Defaults -->
                <LinearLayout
                    android:id="@+id/reset_defaults_button"
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/settings_item_height"
                    android:layout_marginTop="@dimen/space_xl"
                    android:orientation="horizontal"
                    android:gravity="center"
                    android:clickable="true"
                    android:focusable="true"
                    android:background="?attr/selectableItemBackground">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/tips_settings_reset_defaults"
                        android:textSize="16sp"
                        android:textColor="@color/color_text_secondary" />
                </LinearLayout>
            </LinearLayout>

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
